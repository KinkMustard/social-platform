scalar Upload

input CreateListingInput {
  name: String!
  picture: Upload
  category: String!
  description: String!
  upvotes: Int!
  downvotes: Int!
  latitude: Float!
  longitude: Float!
  amenities: [String!]!
}

type Mutation {
  createListing(input: CreateListingInput!): Boolean!
}
