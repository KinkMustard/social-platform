scalar Upload

input CreateListingInput {
  name: String!
  picture: Upload
  description: String!
  upvotes: Int!
  downvotes: Int!
}

type Mutation {
  createListing(input: CreateListingInput!): Boolean!
}
