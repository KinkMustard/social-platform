scalar Upload

input CreateListingInput {
  name: String!
  picture: Upload
  category: String!
  description: String!
  upvotes: Int!
  downvotes: Int!
  amenities: [String!]!
}

type Mutation {
  createListing(input: CreateListingInput!): Boolean!
}
