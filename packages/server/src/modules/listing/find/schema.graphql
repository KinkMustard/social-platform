type Listing {
  id: ID!
  name: String!
  datePosted: String!
  description: String!
  upvotes: Int!
  downvotes: Int!
  pictureUrl: String
  owner: User!
}

type Query {
  findListings: [Listing!]!
}
