type Listing {
  id: ID!
  name: String!
  category: String!
  description: String!
  upvotes: Int!
  downvotes: Int!
  amenities: [String!]!
  pictureUrl: String
  owner: User!
}

type Query {
  findListings: [Listing!]!
}
