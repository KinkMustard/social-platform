input UpdateListingInput {
  name: String
  picture: Upload
  pictureUrl: String
  description: String
  upvotes: Int
  downvotes: Int
  amenities: [String!]
}

type Mutation {
  updateListing(listingId: String!, input: UpdateListingInput!): Boolean!
}
